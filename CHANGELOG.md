# Changelog

## 0.2.0

- More reliable click hit-testing for dotted path underlines (pixel rects).
- Match paths after fullwidth Chinese punctuation (`：` etc.).
- Open via `windowsBin` absolute paths + PowerShell `Invoke-Item` fallback when PATH lacks `/mnt/c/Windows`.

## 0.1.0

- Initial clickable WSL path → Windows open.
